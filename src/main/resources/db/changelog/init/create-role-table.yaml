databaseChangeLog:
  - changeSet:
      id: create-role-table
      author: Khapal Sofiia
      changes:
        - createTable:
            tableName: roles
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: name
                  type: varchar(255)
                  constraints:
                    nullable: false
                    unique: true
        - addCheckConstraint:
            tableName: roles
            constraintName: chk_roles_name
            checkCondition: "name IN ('ADMIN','USER')"